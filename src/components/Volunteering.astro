---
import Section from "./Section.astro";
import { volunteering } from "@cv";
---

<Section className={Astro.props.className} title="Volunteering">
  <ul class="space-y-4">
    {
      volunteering.map(({ title, institution, startDate, endDate, summary }) => (
        <li class="flex flex-col md:flex-row md:items-baseline">
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
            class="mr-2 hidden h-5 w-5 md:inline-block"
          >
            <circle cx="12" cy="8" r="7" />
            <polyline points="8.21 13.89 7 23 12 20 17 23 15.79 13.88" />
          </svg>
          <div class="relative w-full">
            <time class="text-xs font-bold text-black/50 dark:text-white/50 md:absolute md:right-0 md:top-0">
              {startDate}
            </time>
            <div class="font-medium text-black dark:text-white">{institution}</div>
            <p class="text-xs font-semibold text-black/80 dark:text-white/80">
              {title}
            </p>
            <p class= "space-y-1 text-black/50 dark:text-white/50 mt-2 text-sm">
              {summary}
            </p>
          </div>
        </li>
      ))
    }
  </ul>
</Section>
